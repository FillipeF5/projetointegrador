const express = require('express');
const router = express.Router();
const freteController = require('../controllers/freteController.js')

// router.get('/', freteController.index)


module.exportes = router;